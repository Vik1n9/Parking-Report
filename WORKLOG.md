# 工作進度紀錄

## 2026-06-20

- 將保全頁輸入區調整為計算機式版面，在數字鍵盤上方新增目前輸入值顯示框，並把清除本格、上一格、下一格與刪除集中到模式列下方的四欄功能列，移除鍵盤下方重複功能列以降低首屏高度。
- 放大保全頁填寫順序按鈕與文字、下移頁面數字鍵盤區塊，加入全站按鈕按壓動畫，並讓一樓以上與一樓以下輸入滿 3 位數後自動跳下一格，同時保留手動上一格/下一格。

## 2026-06-19

- 統一保全頁數字鍵盤最後一列的 `全滿`、`未停車` 與數字鍵配色，並放大數字鍵文字、改用更接近計算機按鍵的圓厚字體堆疊。
- 針對保全戶外強光與單手使用情境，將 `guard.html` 從深色低亮度主題改為淺底深字、高對比邊框、明確焦點框與高對比模式，並保留低高度手機首屏操作布局。
- 新增 `PRODUCT.md` 紀錄保全頁的產品脈絡、戶外強光、單手操作與首屏優先等設計原則。
- 擴充 `guard-ui.test.js`，鎖定戶外高對比配色、移除模糊透明底、焦點樣式、高對比 media query 與空 favicon，避免 QA console 出現無關 404。
- 依電話鍵盤習慣調整保全頁鍵盤最後一列為 `全滿 / 0 / 未停車`，移除下方功能列的 `未停車 x`，讓常用狀態直接在數字鍵盤空位操作。
- 調整保全手機回報頁低高度手機版面：完整顯示填寫順序、0~9 數字鍵盤、功能鍵與底部 LINE 操作列，避免輸入時頻繁捲動。
- 緊湊高度下將功能鍵改為三欄單列、底部操作列改為三欄單列，並隱藏次要預覽/歷史卡片，讓主要輸入流程留在首屏。
- 移除保全手機回報頁上方「目前區域／一般輸入」狀態卡，避免與下方填寫順序與頁面數字鍵盤重複。
- 新增 `guard-ui.test.js` 靜態回歸測試，鎖定保全頁不再輸出已移除的上方輸入區 DOM。

## 2026-06-18

- 依使用者核准計畫新增保全手機回報入口 `guard.html`，保全頁專注手機操作，不規劃桌面版。
- 新增共享核心 `parking-core.js` 與 `parking-core.test.js`，涵蓋固定區域順序、LINE 易讀版、中控口播、Excel 值與車塔使用率。
- 保全頁採頁面內建 `0~9` 數字鍵盤，支援車位連續輸入、成數輸入、台車模式、上一格/下一格、未停車與清除本格。
- 保全頁新增「分享到 LINE」與「複製 LINE 回報」，歷史紀錄使用獨立 key `parking_guard_history_v1`，避免與中控頁混用。
- README 保留中控頁與保全頁入口連結；依現場安全需求移除 `index.html` 與 `guard.html` 的彼此切換連結，避免誤入錯頁。
- README 的入口連結改為 GitHub Pages 線上網址，避免從 GitHub README 點擊時開到 HTML 原始碼檢視頁。
